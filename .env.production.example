# Production Environment Variables for Vercel Deployment

# Database Configuration
# Use a production PostgreSQL database (e.g., Neon, Supabase, Railway, or Vercel Postgres)
DATABASE_URL=postgresql://user:password@host:5432/database_name?sslmode=require

# VAPID Keys for Web Push
# Generate these using: npm run generate-vapid-keys
NEXT_PUBLIC_VAPID_PUBLIC_KEY=your_production_vapid_public_key
VAPID_PRIVATE_KEY=your_production_vapid_private_key
VAPID_SUBJECT=mailto:your-email@yourdomain.com

# Application URL (your Vercel deployment URL)
# This will be automatically set by Vercel, but you can override it
NEXT_PUBLIC_APP_URL=https://your-app.vercel.app

# Optional: Analytics
NEXT_PUBLIC_GA_TRACKING_ID=your_google_analytics_id

# Optional: Sentry Error Tracking
SENTRY_DSN=your_sentry_dsn
NEXT_PUBLIC_SENTRY_DSN=your_sentry_dsn

# Node Environment
NODE_ENV=production