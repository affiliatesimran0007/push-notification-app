export const metadata = {
  title: 'Subscribe to Push Notifications',
  description: 'Enable push notifications to stay updated with important alerts and announcements.',
};

export default function SubscribeLayout({ children }) {
  return children;
}